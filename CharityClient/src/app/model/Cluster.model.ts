export interface ClusterDTO{
    clusterLocationId: number,
    postId: number,
    requirementTypeName: string,
    clusterDetails: string,
    cityId: number,
    stateId: number,
    peopleCount: number,
    createdBy: Date,
    pincodeId: number,
    latitude: number;
    longitude: number;
}